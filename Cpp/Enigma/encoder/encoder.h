#ifndef __ENCODER_H__
#define __ENCODER_H__

#include "../components/Switcher.h"
#include "../components/WheelGroup.h"

class encoder
{
private:
    /* data */
public:
    encoder(/* args */);
    ~encoder();
};

encoder::encoder(/* args */);

encoder::~encoder();

#endif
